import SnippetList from './SnippetList.jsx';

function App() {
  // App component is now just a wrapper for the main SnippetList component
  return (
    // We are only rendering the main component
    <SnippetList />
  );
}

export default App;
